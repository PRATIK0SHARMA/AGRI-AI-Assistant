services:
  - type: web
    name: agri-ai-assistant
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn crop.main:app --host 0.0.0.0 --port 10000
    pythonVersion: "3.11.9"  # Explicitly set Python version here